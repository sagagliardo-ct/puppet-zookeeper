---
  zookeeper::port:        '12181'

  zookeeper::configdir:   "%{::boxen::config::configdir}/zookeeper"
  zookeeper::datadir:     "%{::boxen::config::datadir}/zookeeper"
  zookeeper::logdir:      "%{::boxen::config::logdir}/zookeeper"

  zookeeper::executable:  "%{::boxen::config::homebrewdir}/bin/zkServer"

  zookeeper::package: "boxen/brews/zookeeper"
  zookeeper::version: "3.4.6-boxen3"
